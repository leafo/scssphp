
Multiple extends can also be written using a comma-separated list of selectors.
For example, @extend .error, .attention is the same as @extend .error;
@extend.attention.

& (parent selector)
#{interpolation} in selectors, property names

match operator results
make formatter match scss's output

css hacks (find out what they are)
built in functions

check scoping:
  * setting an existing variable should write in that variable's scope?
  * do block statements work in their own scope?

